#!/bin/bash
#
# Run geoclue
#/usr/lib/geoclue-2.0/demos/agent &

#
# Check geoclue
#if ! /usr/lib/geoclue-2.0/demos/where-am-i | grep Latitude; then
#  redshift -l $(curl -s "https://location.services.mozilla.com/v1/geolocate?key=geoclue" | awk 'OFS=":" {print $3,$5}' | tr -d ',}')
#else
  #
  # Run redshift
  redshift
#fi
